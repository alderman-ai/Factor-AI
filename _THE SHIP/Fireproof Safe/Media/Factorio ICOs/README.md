# Factorio ICOs

Windows folder icons. Applied through `desktop.ini`, which is gitignored — so
the icons travel with the repo but the assignments do not.

Named for the component, not the folder they are assigned to, so one icon can
be reused when the naming changes.
