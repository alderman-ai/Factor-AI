# Unstructured Entries

Real signal that has not been given a shape yet.

The holding pen between intake and the Knowledge Base: something worth keeping
turned up, but it is not yet a failure-solution pair and forcing it into one
would invent the missing half.

Something leaves here in one direction only — into `Knowledge Base/`, once the
missing half shows up. Nothing is promoted by tidying.

Currently empty.
