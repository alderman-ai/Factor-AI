# The Guy

**Type**: World Objective — the value used in a New World Coordinates form's
`World Objective` frontmatter field. This page exists so the variable alone
conveys the entire meaning.

**First used**: Run 002

The playable actor of V2. He starts inside the Ship. His goal is to get out.

**Win condition (operator ruling, 2026-08-18):** the folder `The Guy/` is
git-moved from `_THE SHIP/` to the repository root. That move is the
observable, falsifiable moment The Guy is out of the ship.
