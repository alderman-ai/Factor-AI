# Hitchhiker's Guide — INDEX

| Folder | What is in it |
|---|---|
| `Instruction Manual/` | The canon *about* the project rather than the machinery — including `Purpose of Factor-AI/`, the background lifted out of root `CLAUDE.md` and turned into an on-demand reading list. |
| `Ontology/` | The long version of the rules. How components are defined and how two of them meet. `The Crate.md`, `Intake Primitives.md`. |
| `Schema/` | Master forms that **scripts consume**. `New World Coordinates.md` is the one the loop stamps. `The Guy.md`. |
| `Human Actions/` | *(unrouted — see below)* Action instances with `Pathing` / `Container` frontmatter and a checkbox set. `Simple Action.md`. |
| `Templates/` | Empty. Deliberately left alone for now, per operator instruction. |

This file accretes. `README.md` does not.

---

**`Human Actions/` has no README and this is the only thing in the sweep left
unwritten.** It appeared while the routing survey was running. What is in it is
clear enough to route; what is not clear is what separates it from `Ontology/`,
which already carries an *Action* tier in `Intake Primitives.md` covering the
same verbs. One question unblocks it: is `Human Actions/` the **instances** of
the Action primitive, with `Ontology/` holding its **definition** — or is it
something else? Answer it and the README writes itself.
